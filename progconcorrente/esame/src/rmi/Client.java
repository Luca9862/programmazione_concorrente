package rmi;
/*
    Nel main del client aggiungere:
    Registry reg = LocateRegistry.getRegistry(1099);
    OCInterface server = (OCInterface) reg.lookup("server");

    e poi usare server come se fosse un oggetto locale sotituendolo nelle chiamate ai metodi dell'oggetto condiviso
 */
public class Client {
}
